doctype html
html(lang="en")
	include include/head.pug
	body
		.container
			h1
				| ensemble 
				small watch videos, together
			form
				fieldset
					label(for="name") your name
					input(type="text" placeholder="name")

					label(for="room id") room id
					input(type="text" placeholder="id")
					button join or create a room
